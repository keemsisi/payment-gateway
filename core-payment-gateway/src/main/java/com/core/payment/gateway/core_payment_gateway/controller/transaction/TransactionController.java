package com.core.payment.gateway.core_payment_gateway.controller.transaction;

import lombok.AllArgsConstructor;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

@AllArgsConstructor
@RestController
@RequestMapping(value = "/v1/api/transactions")
public class TransactionController {
    //create api for fetch bank transaction
    //create api to get transaction by reference
}
